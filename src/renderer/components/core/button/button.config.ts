export enum DisplayStyle {
  Primary,
  Secondary,
  IconOnly1,
  IconOnly2,
  IconOnly3,
  Link1,
  Link2,
  Link3,
  Link4,
}

export enum Type {
  Button = "button",
  Submit = "submit",
  Reset = "reset",
}

export enum Size {
  FixedSmall,
  FixedMedium,
  FixedBig,
}
