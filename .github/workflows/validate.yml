name: Automatic Quality Checks with Artifacts to nexus

on:
  push:
    branches:
      - nexus-poc

jobs:
  validate:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.17.3]

    timeout-minutes: 20

    steps:
      - name: tests env
        run: printenv