{
    "name": "contentful-lambda",
    "description": "Mudita's center contentful lambda",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "contentful": "^8.1.7"
    },
    "devDependencies": {
        "aws-sdk": "^2.815.0",
        "jest": "^26.6.3",
        "uuid": "^8.3.2"
    },
    "scripts": {
        "test": "jest --roots __tests__/unit",
        "integ-test": "jest --roots __tests__/integration"
    }
}
