export enum RadioSize {
  Small = "Small",
  Basic = "Basic",
  Big = "Big",
}

export enum TestId {
  Wrapper = "wrapper",
  CustomInput = "custom-input",
  CheckedIcon = "checked-icon",
  BasicLabel = "label",
}
